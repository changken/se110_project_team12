import React from 'react';
import { render, screen } from './TestUtils';
import GitlabOauth from './GitlabOauth';
import GitlabOauthRedirect from './GitlabOauthRedirect';

// test('test GitlabOauthRedirect', () => {
//   render(<GitlabOauthRedirect />);
//   const title = screen.queryByText(/Redirect url here!/i);
//   const accessTokenTitle = screen.queryByText(/access token: undefined/i);

//   expect(title.textContent).toBe('Redirect url here!');
//   expect(accessTokenTitle.textContent).toBe('access token: undefined');
// });

// test('test GitlabOauth', () => {});
